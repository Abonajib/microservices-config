server:
  port: 8072

# إعدادات الـ Actuator عشان تشوف الـ Routes وتراقب الخدمة
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    gateway:
      enabled: true
  info:
    env:
      enabled: true
